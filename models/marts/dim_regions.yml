version: 2

models:
  - name: dim_regions
    description: "A dimensional table aggregating sales reasons for each sales order."
    columns:
      - name: salesorderid
        description: "Unique identifier for each sales order."
        tests:
          - not_null
          - unique
      - name: reason_name_aggregated
        description: "Aggregated sales reasons for each sales order."
